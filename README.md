# webpack-sensible

Add sensible default configs to webpack.

## Usage

```javascript
const WebpackSensible = require('sensible');

module.exports = {
  ...,
  plugins: [
    new WebpackSensible,
  ]
}
```
